export declare function mergeStrings(originalString: string, newStrings: string[]): string;
/**
 *
 * @param original
 * @param modifications
 * @returns the appropiate result of the merge
 */
export declare function mergeResult<A>(original: A, modifications: A[]): A;
